{
  "id": "pipeline",
  "title": "Pipeline",
  "description": "Interviews the user about the pipeline's kind (test/validation/build/deploy), platform, stack, and deploy targets before generating a CI/CD pipeline — so the config matches real infrastructure, pins its versions, and never invents a secret.",
  "version": "0.1.0"
}
